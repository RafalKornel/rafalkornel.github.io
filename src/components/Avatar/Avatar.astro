---
import { Image } from "astro:assets";
import avatarImage from "./avatar.png";

interface Props {
  variant: "small" | "large";
}

const { variant } = Astro.props;

const diameter = variant === "small" ? 24 : 264;
---

<Image
  width={diameter}
  height={diameter}
  class="rounded-full"
  src={avatarImage}
  alt="Rafał Kornel"
/>
